//
// PayoutCreateRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct PayoutCreateRequest: Codable, JSONEncodable, Hashable {

    public enum Channel: String, Codable, CaseIterable {
        case instapay = "instapay"
        case pesonet = "pesonet"
    }
    public static let referenceIdRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    public static let sourceAccountIdRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    /** Client-provided reference for the payout. */
    public var referenceId: String
    /** Channel to use for the payout. */
    public var channel: Channel
    /** Amount in centavos. */
    public var amount: Int
    /** Org bank account ID to debit. */
    public var sourceAccountId: String
    /** Destination account details. */
    public var destination: PayoutDestination
    public var purpose: String?
    public var metadata: [String: AnyCodable]?

    public init(referenceId: String, channel: Channel, amount: Int, sourceAccountId: String, destination: PayoutDestination, purpose: String? = nil, metadata: [String: AnyCodable]? = nil) {
        self.referenceId = referenceId
        self.channel = channel
        self.amount = amount
        self.sourceAccountId = sourceAccountId
        self.destination = destination
        self.purpose = purpose
        self.metadata = metadata
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case referenceId = "reference_id"
        case channel
        case amount
        case sourceAccountId = "source_account_id"
        case destination
        case purpose
        case metadata
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(referenceId, forKey: .referenceId)
        try container.encode(channel, forKey: .channel)
        try container.encode(amount, forKey: .amount)
        try container.encode(sourceAccountId, forKey: .sourceAccountId)
        try container.encode(destination, forKey: .destination)
        try container.encodeIfPresent(purpose, forKey: .purpose)
        try container.encodeIfPresent(metadata, forKey: .metadata)
    }
}

